# Chapter 13 — Factory, Builder, Abstract Factory → Smart Constructors and Validated Construction

> **Placeholder** — This chapter is planned but not yet written.

*Parse, don't validate — now backed by the full ADT toolkit.*

## Planned Topics

- The OO construction problem: invalid objects that pass the constructor
- Smart constructors from Chapter 6, revisited in a pattern context
- Builder pattern vs. copy-with (Kotlin `copy()`, Swift mutating structs, spread operators)
- Accumulating validation errors: `Result<T>` with applicative combination
- Worked example: configuration parsing that cannot produce invalid config
